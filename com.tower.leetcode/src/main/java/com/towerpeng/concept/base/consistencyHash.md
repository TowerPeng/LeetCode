一致性hash算法：
一致性hash算法是一种分布式算法，用于解决数据分布和负载均衡问题，它通过将数据和节点映射到一个哈希环上，实现了数据在节点之间的均匀分布和最小化数据迁移。
如果每次按hash取模新增或者删除节点，会导致所有节点都产生变化，影响数据分布，因此一致性hash算法会记录节点的hash值，当节点新增或者删除时，只会影响新增或者删除的节点，不会影响其他节点。
但是节点少会导致负载不均衡，热点数据集中在几个节点上，无法完全解决数据倾斜问题。

16384节点，数据量1000w，1000w/16384=7.3