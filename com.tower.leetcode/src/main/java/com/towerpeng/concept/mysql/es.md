MYsql数据如何同步到es
1、业务代码实现双写，每次修改Mysql数据的时候都往es写数据，耦合度高，需求变更有额外的更新成本
2、消息队列做异步的数据同步，也就是数据发生变更的时候，触发mq消息，mq监听到消息之后，将数据同步到es，降低了耦合度，还能失败重试，引入第三方组件增加了系统复杂度
3、阿里开源Cannal组件，监听mysql的binlog日志，捕获数据变更事件，实时推送到es，实现方式与MQ很类似，他不需要切入到业务系统，灵活性更高

mq和cannal组件可以应对大部分场景下的数据同步