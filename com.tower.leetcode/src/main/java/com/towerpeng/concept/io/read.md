IO是什么：
IO是输入输出，输入输出是计算机处理数据的基本单位，是计算机与外部设备的通信。

BIO：（一个线程）
同步阻塞IO，线程会一直阻塞等待操作完成。
基于流模型，传统的字节流和字符流。
适合连接数少的场景。
NIO：（多路复用器监听多个请求）
非阻塞IO，线程可以去做其他任务，当IO操作完成时，会通知线程。
基于缓冲区和通道，使用缓冲区完成数据读写操作。
适合连接数多但连接时间短的场景。
AIO：（操作系统完成，不需要线程池和多路复用器）
也是非阻塞IO，不需要用户关注线程IO事件，由操作系统通过回调处理。
基于事件和回调机制，异步回调。
适合连接数多且连接时间长的场景。

